{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter @chris-towles/blog build && mkdir -p .vercel && cp -r packages/blog/.vercel/output .vercel/output",
  "installCommand": "pnpm install"
}
