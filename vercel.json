{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm build && find . -maxdepth 4 -name config.json -path *vercel* 2>/dev/null && pwd && ls .vercel/output 2>/dev/null; ls packages/blog/.vercel/output 2>/dev/null",
  "installCommand": "pnpm install",
  "framework": null
}
